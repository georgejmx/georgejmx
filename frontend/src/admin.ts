import "virtual:windi.css";

const inputBox: HTMLDivElement = document.getElementById(
  "inputs-box"
) as HTMLDivElement;
inputBox.innerHTML =
  "<p>Here is where the dynamically generated inputs go :)</p>";

export {};
