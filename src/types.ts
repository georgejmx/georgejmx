export type Fascination = {
  color: string;
  intensity: number;
  name: string;
};

export type Panel = "home" | "projects";

export type Project = {
  description: string;
  imageName: string;
  name: string;
  url: string;
};
