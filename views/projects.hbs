{{#each projects}}
    <div
        class="flex flex-col max-w-sm border-1 overflow-hidden border-yellow-500 bg-black p-2 mb-6"
    >
        <img src="images/projects/{{this.imagename}}" class="max-h-70 p-2" />
        <div class="font-bold text-xl mb-2 font-mono text-yellow-500">
            {{this.name}}
        </div>
        <a
            class="font-mono text-cyan-200 hover:text-cyan-300 mb-2 italic"
            href="{{this.url}}"
        >{{this.urlname}}</a>
        <p class="font-mono text-white text-sm py-2">
            {{this.description}}
        </p>
    </div>
{{/each}}
<p class="text-white font-mono mb-6">
    Many more projects can be found on my
    <a
        href="https://github.com/georgejmx/"
        class="font-mono text-yellow-200 hover:text-yellow-500"
    >GitHub</a>
</p>